import multerConfig, { uploadImage, uploadMusic } from "./configs/multerConfig";

export { multerConfig as default, uploadImage, uploadMusic };

export { ALLOWED_MIMES } from "./constants/uploadConstants";
